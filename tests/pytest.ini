[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    slow: Langsame Tests (z.B. grosse Dateien)
addopts =
    -v
    --tb=short
    -ra
